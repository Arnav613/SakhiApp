import 'package:flutter/material.dart';

class SakhiColors {
  // Primary palette
  static const Color deep   = Color(0xFF3B1040); // deep plum — headers, nav
  static const Color rose   = Color(0xFFC4527A); // raspberry rose — primary accent
  static const Color drose  = Color(0xFF922050); // dark rose — pressed states
  static const Color gold   = Color(0xFFC9916A); // rose gold — highlights
  static const Color sage   = Color(0xFF5E8A7A); // sage green — health/safety accent
  static const Color sageP  = Color(0xFFEAF4F1); // sage pale — health bg

  // Backgrounds
  static const Color blush  = Color(0xFFFEF3F7); // card backgrounds
  static const Color vblush = Color(0xFFFDF8FA); // alternate row / page bg
  static const Color petal  = Color(0xFFE8A0B4); // borders, dividers

  // Feature accents
  static const Color amber  = Color(0xFFC0784A); // journal / amber
  static const Color amberP = Color(0xFFFDF2EE); // journal bg

  // Neutrals
  static const Color white  = Color(0xFFFFFFFF);
  static const Color black  = Color(0xFF1A1A1A);
  static const Color gray   = Color(0xFF444444);
  static const Color lgray  = Color(0xFF999999);
  static const Color border = Color(0xFFE8E0EC);

  // Cycle phase colors
  static const Color menstrual  = Color(0xFFF5E6F0);
  static const Color follicular = Color(0xFFE8F0FB);
  static const Color ovulatory  = Color(0xFFE6F7EE);
  static const Color luteal     = Color(0xFFFFF8E8);

  static const Color menstrualDark  = Color(0xFF8B2560);
  static const Color follicularDark = Color(0xFF1A3A8A);
  static const Color ovulatoryDark  = Color(0xFF1A5E30);
  static const Color lutealDark     = Color(0xFF7A4800);
}
